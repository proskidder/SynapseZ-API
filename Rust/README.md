# Dependencies

```rust
reqwest = { version = "0.12.2", features = ["blocking"] } 
sysinfo = "0.30.7"
```

Any version could possibly work, it just needs blocking features for reqwest and process features for sysinfo, nothing much

# Importing

```rust
mod synapsezapi;
```

# Usage
U should have enough rust knowledge, i won't tell you how to use every function

functions are like in C# version, thx np.

Were untested tho so if something doesn't work as expected just tell me or sum
\
or don't lol idc